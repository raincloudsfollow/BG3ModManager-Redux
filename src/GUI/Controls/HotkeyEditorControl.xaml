<UserControl
	x:Class="DivinityModManager.Controls.HotkeyEditorControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
	xmlns:adonisUI="clr-namespace:AdonisUI;assembly=AdonisUI"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:DivinityModManager.Controls"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	x:Name="HotkeyUserControl"
	d:DesignHeight="75"
	d:DesignWidth="200"
	KeyboardNavigation.IsTabStop="False"
	mc:Ignorable="d">
	<UserControl.Style>
		<Style BasedOn="{StaticResource {x:Type UserControl}}" TargetType="UserControl" />
	</UserControl.Style>
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="../Themes/MainResourceDictionary.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Button
		x:Name="HotkeyButton"
		Grid.Column="1"
		HorizontalContentAlignment="Center"
		VerticalContentAlignment="Center"
		Click="HotkeyButton_Click">
		<Button.Style>
			<!--
				A self-contained Template, not a BasedOn self-key bridge to the type's default style;
				that bridge does not reliably resolve AdonisUI's real default Button style from
				this UserControl's own merged resources, and silently falls back to the raw unstyled
				WPF template (see the same issue fixed for TextBox in MainResourceDictionary.xaml).
			-->
			<Style TargetType="Button">
				<Setter Property="Background" Value="{DynamicResource ReduxSurfaceMutedBrush}" />
				<Setter Property="BorderBrush" Value="{DynamicResource ReduxBorderStrongBrush}" />
				<Setter Property="BorderThickness" Value="1" />
				<Setter Property="Foreground" Value="{DynamicResource ReduxTextPrimaryBrush}" />
				<Setter Property="Padding" Value="8,4" />
				<Setter Property="FocusVisualStyle" Value="{x:Null}" />
				<Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="{DynamicResource Redux.CornerRadius.Control}" />
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="Button">
							<Border x:Name="ButtonChrome"
								Background="{TemplateBinding Background}"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="{TemplateBinding BorderThickness}"
								CornerRadius="{TemplateBinding adonisExtensions:CornerRadiusExtension.CornerRadius}">
								<ContentPresenter Margin="{TemplateBinding Padding}"
									HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
									VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
									RecognizesAccessKey="True" />
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="IsMouseOver" Value="True">
									<Setter TargetName="ButtonChrome" Property="BorderBrush" Value="{DynamicResource ReduxAccentHoverBrush}" />
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
				<Style.Triggers>
					<DataTrigger Binding="{Binding IsFocused, ElementName=HotkeyTextBox}" Value="True">
						<Setter Property="Background" Value="{DynamicResource ReduxSelectionBrush}" />
						<Setter Property="BorderBrush" Value="{DynamicResource ReduxAccentBrush}" />
					</DataTrigger>
					<MultiDataTrigger>
						<MultiDataTrigger.Conditions>
							<Condition Binding="{Binding IsFocused, ElementName=HotkeyTextBox}" Value="True" />
							<Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
						</MultiDataTrigger.Conditions>
						<MultiDataTrigger.Setters>
							<Setter Property="Background" Value="{DynamicResource ReduxSelectionBrush}" />
							<Setter Property="BorderBrush" Value="{DynamicResource ReduxAccentHoverBrush}" />
						</MultiDataTrigger.Setters>
					</MultiDataTrigger>
				</Style.Triggers>
			</Style>
		</Button.Style>

		<Button.Content>
			<TextBox
				x:Name="HotkeyTextBox"
				Background="Transparent"
				BorderBrush="Transparent"
				BorderThickness="0"
				Padding="0"
				Focusable="{Binding Path=Hotkey.CanEdit, ElementName=HotkeyUserControl, FallbackValue=True}"
				IsHitTestVisible="False"
				IsReadOnly="True"
				IsReadOnlyCaretVisible="False"
				IsUndoEnabled="False"
				KeyboardNavigation.IsTabStop="False"
				PreviewKeyUp="HotkeyTextBox_PreviewKeyUp"
				PreviewMouseRightButtonDown="HotkeyTextBox_PreviewMouseRightButtonDown"
				Text="{Binding Path=Hotkey.DisplayBindingText, ElementName=HotkeyUserControl, Mode=OneWay}">
				<TextBox.ContextMenu>
					<ContextMenu IsHitTestVisible="False" Visibility="Collapsed" />
				</TextBox.ContextMenu>
			</TextBox>
		</Button.Content>
	</Button>

</UserControl>
